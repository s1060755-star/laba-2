# 🚀 Оптимізації та Безпека

## Виконані покращення

### 📊 Backend Оптимізації

#### 1. Оптимізація бази даних (database.py)

**SQLite налаштування:**
- `PRAGMA journal_mode=WAL` - Write-Ahead Logging для кращої concurrency
- `PRAGMA synchronous=NORMAL` - Баланс між швидкістю та безпекою
- `PRAGMA cache_size=10000` - Збільшений кеш (10MB)
- `PRAGMA temp_store=MEMORY` - Тимчасові таблиці в пам'яті
- `PRAGMA mmap_size=268435456` - Memory-mapped I/O (256MB)

**Індекси для швидкого пошуку:**
- `idx_dish_price` - Індекс на ціну страв
- `idx_dish_name` - Індекс на назву страв
- `idx_orders_phone` - Швидкий пошук замовлень по телефону
- `idx_orders_created` - Сортування замовлень по даті
- `idx_accounts_email` - Унікальний індекс на email (запобігає дублікатам)
- `idx_accounts_phone` - Пошук по телефону
- `idx_favourites_dish` - Оптимізація для улюблених страв
- `idx_favourites_account` - Пошук улюблених по користувачу
- `idx_favourites_unique` - Запобігання дублікатам улюблених
- `idx_admin_username` - Швидкий пошук адміністраторів

**Очікуваний результат:**
- Запити SELECT до 50-70% швидше
- Краща обробка concurrent requests
- Зменшення I/O операцій

#### 2. HTTP Response Compression (main.py)

**Flask-Compress:**
- Автоматичне стиснення HTML, CSS, JavaScript
- Зменшення розміру відповідей на 60-80%
- Підтримка gzip та deflate

**Використання:**
```python
from flask_compress import Compress
Compress(app)
```

#### 3. Кешування (main.py)

**In-memory cache для статичних даних:**
- Кешування відповідей на 5 хвилин (конфігурується)
- Автоматичне очищення застарілих даних
- Декоратор `@cache_response(ttl=300)`

**Приклад використання:**
```python
@app.route('/menu')
@cache_response(ttl=600)  # 10 хвилин
def menu():
    return render_template('menu.html', dishes=get_all_dish())
```

#### 4. Rate Limiting (main.py)

**Захист від DDoS та надмірного навантаження:**
- Ліміт: 100 запитів на хвилину з одного IP
- Автоматичне очищення старих записів
- HTTP 429 для перевищення ліміту

**Використання:**
```python
@app.route('/api/data')
@rate_limit
def api_endpoint():
    return jsonify(data)
```

---

### 🔐 Безпека

#### 1. Валідація вхідних даних (database.py)

**Функції валідації:**
- `validate_email()` - Перевірка email формату
- `validate_phone()` - Валідація телефонних номерів
- `validate_price()` - Перевірка коректності цін
- `validate_integer()` - Валідація цілих чисел
- `sanitize_string()` - Очищення рядків від небезпечних символів

**Захист від:**
- XSS атак (видалення < >)
- SQL injection (prepared statements)
- Некоректних даних

**Приклад:**
```python
def add_account(first_name, last_name, phone, email):
    # Валідація
    if not validate_email(email):
        raise ValueError("Некоректна email адреса")
    
    email = sanitize_string(email, 200)
    # ... додавання в БД
```

#### 2. CSRF Protection (main.py)

**Cross-Site Request Forgery захист:**
- Генерація унікальних CSRF токенів для кожної сесії
- Валідація токенів при POST запитах
- Автоматична доступність у шаблонах

**Використання в шаблонах:**
```html
<form method="POST">
    <input type="hidden" name="csrf_token" value="{{ csrf_token() }}">
    <!-- форма -->
</form>
```

#### 3. Security Headers (main.py)

**HTTP Security Headers для production:**
- `X-Content-Type-Options: nosniff` - Запобігає MIME sniffing
- `X-Frame-Options: SAMEORIGIN` - Захист від clickjacking
- `X-XSS-Protection: 1; mode=block` - Включає XSS фільтр браузера
- `Strict-Transport-Security` - HTTPS only
- `Content-Security-Policy` - Обмеження завантаження ресурсів

#### 4. Session Security (main.py)

**Налаштування сесій:**
- `SESSION_COOKIE_HTTPONLY=True` - Недоступні для JavaScript
- `SESSION_COOKIE_SAMESITE='Lax'` - CSRF захист
- `SESSION_COOKIE_SECURE=True` (production) - Тільки HTTPS
- Автоматична генерація секретного ключа

#### 5. Додаткові Security Utilities (security.py)

**Middleware та утиліти:**
- `sanitize_html()` - Екранування HTML
- `validate_sql_input()` - Перевірка на SQL injection
- `secure_filename_custom()` - Безпечні імена файлів
- `check_password_strength()` - Валідація міцності паролів
- `log_security_event()` - Логування security подій

**Декоратори:**
- `@require_auth` - Вимагає авторизації
- `@require_admin` - Вимагає прав адміністратора
- `@validate_json_input()` - Валідація JSON даних

---

### ⚡ Frontend Оптимізації

#### 1. Performance Optimizations (static/performance.js)

**Lazy Loading зображень:**
- IntersectionObserver для lazy loading
- Завантаження зображень тільки при появі у viewport
- Зменшення initial page load

**Prefetching:**
- Автоматичний prefetch при hover на посилання
- Швидша навігація

**Debounce та Throttle:**
- Оптимізація обробників подій
- Зменшення кількості викликів функцій

**LocalStorage кеш:**
- Кешування даних меню
- TTL для автоматичного оновлення
- Offline підтримка

#### 2. Service Worker (static/sw.js)

**Offline-first стратегія:**
- Кешування статичних ресурсів
- Network-first для динамічних даних
- Fallback для offline режиму

**Кеш стратегія:**
```
Network First → Cache Fallback → Offline Page
```

#### 3. Оптимізація форм

**Real-time валідація:**
- Debounced валідація при вводі
- Візуальний feedback
- Попередження про незбережені дані

**Performance metrics:**
- Автоматичний збір метрик завантаження
- Виявлення повільних сторінок
- Console warnings для page load > 3s

---

## 📈 Очікувані результати

### Продуктивність:
- ⚡ **50-70% швидше** запити до БД (завдяки індексам)
- 📦 **60-80% менше** розмір передаваних даних (compression)
- 🚀 **2-3x швидше** повторні візити (кешування)
- 📱 **40-60% швидше** initial load (lazy loading)

### Безпека:
- 🔒 **Захист від XSS** атак (sanitization)
- 🛡️ **Захист від CSRF** (токени)
- 🚫 **Захист від SQL injection** (валідація + prepared statements)
- 🔐 **Захист від DDoS** (rate limiting)
- 📝 **Логування security events**

---

## 🔧 Налаштування

### 1. Встановлення залежностей:

```bash
pip install -r requirements.txt
```

### 2. Конфігурація кешування:

У `main.py` можна налаштувати TTL:
```python
CACHE_TTL = 300  # 5 хвилин (за замовчуванням)
```

### 3. Конфігурація Rate Limiting:

```python
RATE_LIMIT_REQUESTS = 100  # Максимум запитів
RATE_LIMIT_WINDOW = 60     # За хвилину
```

### 4. Додавання performance.js до шаблонів:

```html
<script src="{{ url_for('static', filename='performance.js') }}"></script>
```

### 5. Використання CSRF токенів:

Додайте до всіх POST форм:
```html
<input type="hidden" name="csrf_token" value="{{ csrf_token() }}">
```

---

## 🧪 Тестування

### Тестування продуктивності:

```bash
# Apache Bench
ab -n 1000 -c 10 http://localhost:5000/

# Або використовуйте browser DevTools:
# Network tab → Disable cache → Reload
```

### Тестування безпеки:

```bash
# Перевірка security headers
curl -I http://localhost:5000/

# SQL injection тест (повинен бути відхилений)
curl -X POST http://localhost:5000/api/test \
  -d "name=admin' OR '1'='1"
```

---

## 📚 Додаткові рекомендації

### Production:

1. **Використовуйте CDN** для статичних файлів
2. **Налаштуйте Redis** для distributed caching
3. **Використовуйте nginx** як reverse proxy
4. **Включіть HTTP/2** для кращої продуктивності
5. **Налаштуйте monitoring** (Sentry, New Relic)

### Моніторинг:

```python
# Додайте до main.py
import logging
logging.basicConfig(level=logging.INFO)
```

### Database:

Для production розгляньте міграцію на PostgreSQL:
- Краща concurrency
- Більше можливостей
- Надійніше для великих навантажень

---

## 🔗 Корисні посилання

- [Flask Security Best Practices](https://flask.palletsprojects.com/en/2.0.x/security/)
- [OWASP Top 10](https://owasp.org/www-project-top-ten/)
- [Web Performance Best Practices](https://web.dev/fast/)
- [Service Workers](https://developers.google.com/web/fundamentals/primers/service-workers)

---

**Версія**: 1.0  
**Дата**: 15.12.2025  
**Автор**: Optimization & Security Update
